  // establish component local start values **Not used with STATE tracking**
  const standardWholeNumberFormat: Intl.NumberFormatOptions = {
    style: "decimal", 
    useGrouping: true,
    minimumFractionDigits: 0,
    maximumFractionDigits: 0
  };

  export default standardWholeNumberFormat;